#include <stdio>

/**
 * int _isupper - funtion that checks for uppercase character.
 *
 * Return: 0 otherwise
 */

int _isupper(int c)
{
	char alph;

	for (alph = 'a'; alph <= 'z'; alph++)
	{
		_puthar(alph);
	}
	return (0);
}

