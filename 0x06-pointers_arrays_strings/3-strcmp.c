#include "main.h"
#include <string.h>

/**
 * _strcmp - function that compares two strings
 * @s1: first parameter
 * @s2: second parameter
 * Return: integer
 */

int _strcmp(char *s1, char *s2)
{
	int r;

	r = strcmp(s1, s2);
	return (r);
}
