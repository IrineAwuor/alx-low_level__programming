#include "main.h"

/**
 * _strlen - function that returns the length of a string.
 * @s: parameter 
 * Return: length of a string
 */

int _strlen(char *s)
{
	int length = 0;

	while (*str++)
		length++;
	return (length);
}
